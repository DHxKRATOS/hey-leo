'use strict';

/**
 * blacklisted-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::blacklisted-token.blacklisted-token');
